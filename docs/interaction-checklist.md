# 交互清单

自建节点编辑器的真实风险不在功能，在**交互细节的长尾**。连线吸附手感、对齐参考线、多选批量移动、
连线中途松手弹搜索面板、断开连线时另一端跟随鼠标……成熟编辑器打磨了很多年，
用户会下意识觉得理所应当，缺了就觉得"这软件很粗糙"。

这份清单的用途是：**在动工前就知道总共要补多少项**，而不是做到一半才发现还有二十项。

对照基准：ComfyUI（节点图易用性）、Blender Geometry Nodes（编辑器手感）。
「RF」列表示 React Flow 是否白送。

---

## P0 — 不做就没法用

**状态：P0 全部完成。** 第 1–13 项与第 16 项（环检测，提前做掉）于 M1 完成；
第 14–15 项依赖执行状态，于 M2 完成。

| # | 项 | RF | 状态 | 备注 |
|---|---|---|---|---|
| 1 | 画布平移 / 缩放 / Fit view | ✅ | ✅ | |
| 2 | 拖拽移动节点 | ✅ | ✅ | |
| 3 | 从端口拖出连线并落到目标端口 | ✅ | ✅ | |
| 4 | 框选 + 多选批量移动 / 删除 | ✅ | ✅ | |
| 5 | 删除节点自动清理相连边 | ✅ | ✅ | |
| 6 | Minimap | ✅ | ✅ | |
| 7 | 端口类型校验（不兼容不让连） | 🔶 | ✅ | `isValidConnection` 钩子，逻辑自己写 |
| 8 | 端口 / 连线按类型着色 | ❌ | ✅ | 用 manifest 的 `types[].color`，成本极低收益极高 |
| 9 | 节点搜索面板（双击画布 / 空格 / Tab） | ❌ | ✅ | 模糊匹配 + 分类树 + 键盘上下选中 |
| 10 | 参数表单动态生成 | ❌ | ✅ | 见 [operator-manifest.md](operator-manifest.md) 的控件映射表 |
| 11 | 撤销 / 重做 | ❌ | ✅ | 语义化动作定边界 + immer 结构共享快照。**排除纯 UI 操作** |
| 12 | 复制 / 粘贴 / 剪切（保留内部连线） | ❌ | ✅ | 粘贴时重映射 id，位置偏移一点 |
| 13 | 保存 / 打开 / 另存为 + 脏标记 | ❌ | ✅ | |
| 14 | 节点状态高亮 pending/running/done/error/skipped | ❌ | ✅ | custom node 读独立的 execution store；状态用边框色 + 色点，不改底色 |
| 15 | 错误定位到具体参数框 | ❌ | ✅ | 靠 `ExecutionEvent.errors[].paramPath`；红框 + 控件下方的消息 + 检查器顶部的全量诊断 |
| 16 | 环检测 + 拓扑排序 | ❌ | ✅ | 几十行；连线时即时拒绝成环。原计划 M2，M1 提前做掉 |

> RF 图例：✅ 开箱即用 　🔶 提供钩子、逻辑自写 　❌ 完全自建

## P1 — 缺了会被说"粗糙"

**状态：P1 全部完成。** 第 23、27、30 项于 M2 完成，其余于 M3 完成。
每一项在 `scripts/e2e/m3.mjs` 里至少有一条 CDP 断言。

| # | 项 | 状态 | 备注 |
|---|---|---|---|
| 17 | 连线端口吸附（靠近即吸附，容差 ~20px） | ✅ M3 | `connectionRadius=24`。落点偏 16 px 仍然吸得上 |
| 18 | 连线中途松手 → 弹出搜索面板，选中后自动接上 | ✅ M3 | `onConnectEnd` 记住 `pendingFrom` 与方向；拖输入端时新节点接在上游 |
| 19 | 断开输入端连线时，另一端跟随鼠标（而不是直接删掉） | ✅ M3 | `edgesReconnectable` + `onReconnect`；落到空白处才断开并复用 #18 |
| 20 | 拖线时不兼容端口置灰、兼容端口高亮 | ✅ M3 | `ui.pendingFrom` + `compatiblePorts`，端口自己读，不重建节点数组 |
| 21 | 拖节点到已有连线上 → 自动插入到中间 | ✅ M3 | 拖动结束时对单个选中节点做边命中（12 px）；**有且仅有一对**兼容端口才插 |
| 22 | 对齐参考线 / 网格吸附 | ✅ M3 | `snapGrid=[8,8]`，Shift 临时关掉；参考线自绘在 `ViewportPortal` 里 |
| 23 | 上游参数改动 → 下游节点标 stale（虚线框） | ✅ M2 → M3 精确化 | M3 起精确到节点：`plan_graph` 的 cacheKey 与 `run_started` 对比（[ADR-0007](adr/0007-cache-authority.md)） |
| 24 | Reroute 节点（连线整理） | ✅ M3 | `util.reroute` 是 `Any → Any` 的**普通算子**；在**边的右键菜单**里「在此插入 Reroute」。原来的双击连线中点已让给 #39 的查看器（edge-peek-plan P1） |
| 25 | 节点折叠 / 重命名 / 静音（bypass） | ✅ M3 | Ctrl+E / 双击标题 / Ctrl+M。bypass 是执行语义，进 doc 也进撤销栈 |
| 26 | 参数右键：重置为默认 / 复制值 / 粘贴值 | ✅ M3 | 外加「复制路径名」，给 M4 的 CLI `--set` 用；粘贴按参数类型校验形态 |
| 27 | Run to node（只跑上游闭包） | ✅ M2 | 右键节点 → 运行到此节点；Shift+F5 跑到选中节点 |
| 28 | 数字框拖动改值（drag-to-change） | ✅ M3 | 水平拖 = step，Shift ×10，Alt ÷10；整段一条撤销 |
| 29 | 快捷键体系 + 可查询的快捷键面板 | ✅ M3 | 键表只有 `lib/keymap.ts` 一张，处理器和 `?` 面板都从它生成（E7） |
| 30 | 选中节点 → 3D 视图预览其输出 | ✅ M2 → M3 补齐 | M3 加着色模式（强度/高度/单色）、色带与范围、钉住、导出 PNG。接入 gap 之后补：节点自己没有点云输出时，沿输入边取上游最近的一片云当**底图**，几何叠在它上面，栏上标「底图：<节点名>」；取景按「底图云 + 几何」的联合包围盒 |

## P2 — 可以先不做

**状态：#31 #33 #34 #36 #37 于 M4 完成，#32 以子图取代（见备注），#39 于连线查看器计划完成，#40 于节点运行按钮计划完成，#41 #42 于参数配方计划 P2 完成，#43 于 P3 完成（P4 接上 CLI / MCP / 宿主），#44 于 P4 顺手修。**
每一项在 `scripts/e2e/m4.mjs`、`scripts/e2e/peek.mjs`、`scripts/e2e/noderun.mjs` 或 `bridge/src/cli.rs` 的测试里至少有一条断言。

| # | 项 | 状态 | 备注 |
|---|---|---|---|
| 31 | 子图 / 复合算子 | ✅ M4 | compile 期展开成平图，路径式节点 id，参数提升，库算子目录（[ADR-0010](adr/0010-subgraph-by-expansion.md)） |
| 32 | 节点分组框（Frame）+ 整组折叠 | ✅ M4（以子图取代） | 子图节点本身就是「一个可折叠的框」，而且它有语义：能提升参数、能存成库算子、能整体静音。再做一个纯 UI 的框是两套心智，见 m4-acceptance 的「偏离与决策」 |
| 33 | Live preview（拖参数实时出结果） | ✅ M4 | 源头抽稀的 preview run，独立缓存命名空间（[ADR-0011](adr/0011-preview-as-decimated-run.md)）；拖动到渲染 < 100 ms |
| 34 | 批量扫参 / 参数网格搜索 | ✅ M4 | `lyflow sweep`：笛卡尔积 + 进程内缓存让上游只算一次；可出 CSV |
| 35 | 两个节点输出并排对比 | ⬜ M5 | |
| 36 | 图的 diff / 版本对比 | ✅ M4 | `lyflow diff`：合并默认值后比较，忽略 `ui`；只移动节点时输出为空 |
| 37 | 大图性能（数百节点） | ✅ M4 | `onlyRenderVisibleElements`（> 80 节点才开）+ 执行事件 16 ms 合并；300 节点打开 < 1 s、拖动 ≥ 30 fps |
| 38 | 协作 / 多人编辑 | ⬜ 不计划 | |
| 39 | 双击连线查看内容（Edge Peek） | ✅ S1–S7 | 四种视图：3D 点云 / 正交 2D（剖面 + 2D 几何）/ 张量图像 / 键值表 + 原始 JSON。可多开、可拖动、可锁定快照；上限 **6 窗，其中带 WebGL 的 4 个**。没有运行在跑时 `Esc` 关最前面的一个。计划见 [edge-peek-plan.md](edge-peek-plan.md)，张量 / 下标的 ABI 见 [ADR-0019](adr/0019-output-tensor-and-indices-over-abi.md)；验收在 `scripts/e2e/peek.mjs` |
| 40 | 节点运行按钮（标题栏右端的小圆圈） | ✅ node-run | 修订一起单击 = 智能运行（`targets`：本节点 + 缺结果或过时的上游，下游不动，计划外节点的结果照样挂着可取），hover 预告会一并跑哪些上游或「已是最新」；Shift+单击 = 强制重算（core 的 `force`，C ABI v11）。右键三项：运行到此 / 强制重算此节点 / 仅此节点（`isolate`：上游只用已有结果，不齐时置灰；绕过去由 core 在开跑前拒绝，`upstream_not_ready` toast + 缺结果的上游闪一下）。自己发起的那次运行中点它是停止，别的运行中点它是抢占。计划见 [node-run-plan.md](node-run-plan.md)，验收在 `scripts/e2e/noderun.mjs` |
| 41 | 参数面板（一张图的全部参数在一处看、改） | ✅ param-recipe P2 | 工具栏「参数」或 **Ctrl+Shift+P** 开关；画布右侧与画布并排，开着时替代 Inspector；拖分栏改宽（记在 localStorage），「□」最大化（画布收起）。页签：按节点 / 配方矩阵 / 配方管理（后两个 P3）。按节点页：顶部「图参数」分组（改值、改规格、解除绑定、删除），然后按节点 → group 列出全部参数，advanced 默认收起，子图实例可展开进定义（标「子图定义 · N 个实例共享」），库算子只读。每行 label（改过有蓝点）/ 控件 / 恢复与「纳入配方」书签；搜索（参数名、label、节点、值）+ chip（全部 / 已改动 / 配方 / 诊断 / 类型）。画布选中 ↔ 面板定位双向联动；ROI 行有缩略图、点「拖框」进 2D 拖框视图。1000 参数打开 < 300 ms（虚拟化）。计划见 [param-recipe-plan.md](param-recipe-plan.md) P2，验收在 `scripts/e2e/params_p2.mjs` |
| 42 | transform / curve 参数控件 | ✅ param-recipe P2 | transform：平移 xyz + 旋转 xyz（度，R = Rz·Ry·Rx），可切 4×4 矩阵；curve：小画布拖控制点（双击加点、双击点删除）+ 控制点列表 + 线性 / 平滑。值格式见 [operator-manifest.md](operator-manifest.md)「transform 与 curve 的值」。面板与 Inspector 同一份控件 |
| 43 | 参数配方（同一张图切换不同的一组参数值） | ✅ param-recipe P3 | 配方 = 顶层图参数的稀疏覆盖，每个配方一个 `<名字>.lyflow-recipe.json`，放在图文件旁边的 `<图名>.recipes/`（`index.json` 记默认与顺序）。工具栏右侧的**配方下拉框**切换（「基础」+ 各配方 +「新建配方…」），切换不弹窗、不进撤销，有没存的改动时旁边一个橙色小圆点；**Ctrl+S** 一次存图与所有改过的配方，存前检测文件是否被外部修改（覆盖 / 重新载入 / 取消）。选着配方时改已纳入的参数写进配方，改没纳入的照常改图并提示「改为只在本配方生效」；被覆盖的行有橙色竖条、「配方 · 基础 X」、恢复基础 / 写回基础。参数面板的**配方矩阵**（三态、只看差异、单元格编辑、多选复制、隐藏列）与**配方管理**（新建、复制、重命名、删除、设为默认、导入、导出、四类失配报告与按建议修复）。改配方与改图同一个撤销栈。有失配（多出、类型不符、越界）的配方不能运行。格式与规则见 [recipe.md](recipe.md)，验收在 `scripts/e2e/params_p3.mjs`。P4：CLI `--recipe`（`run` / `validate` / `plan` / `params` / `eval` / `patch`，失配退出码 4）与 `lyflow recipes`、MCP 的 `recipe` 与 `list_recipes`、宿主按名字切换（[embedding.md](embedding.md)「按名字切换配方」）；CLI 与编辑器选同一个配方跑真实 gap 图逐位相同，验收在 `scripts/e2e/params_p4.mjs` |
| 44 | 窄窗口的工具栏（1280–1440 宽） | ✅ param-recipe P4 | 1440 宽及以下：间距与按钮内边距收窄、撤销 / 重做只留箭头（字在 title / aria-label）、文件名收起（悬停图名看路径），再挤先收运行区右侧的状态字（错误文字省略号），图名框至少完整放下 8 个汉字、有富余长到 180 px；1280 / 1366 / 1440 三个宽度下工具栏不溢出、配方下拉框在窗口里。验收在 `scripts/e2e/params_p4.mjs` |

---

## 工作量估计

| 分档 | 内容 | 估计 |
|---|---|---|
| 便宜（几百行内） | 拓扑排序+环检测、端口类型校验、序列化、状态高亮、类型着色 | 各几十到一两百行 |
| 中等 | 参数表单动态生成、撤销重做、复制粘贴、节点搜索面板 | 每项 1–3 天 |
| 贵 | 子图、live preview、大图性能 | 各一周以上，可延后 |

核心跑通（P0 全部）约 1–2 周；打磨到用着顺手（P1 大部分）约 1–2 个月。
长尾主要在子图和撤销重做的边界情况。

## 验收方式

每完成一档，找一个没参与开发的人，给他一句话任务（"把这个 pcd 降采样后去平面，看结果"），
不做任何讲解，观察他卡在哪。卡住的地方基本都能对应到上表里某个没做的项。

**M1–M4 的实际做法**：不写前端单元测试（CLAUDE.md），改为用 CDP 驱动真实运行的
Tauri app 断言。脚本在 [`scripts/e2e/`](../scripts/e2e/)，`pnpm e2e` 一条命令跑完：
M2 的分组在 `run.mjs`，M3 的在 `m3.mjs`，M4 的在 `m4.mjs`，页面侧动作共用 `page.mjs`。
覆盖演示 pipeline、错误定位、取消、中文路径（stale 与 Run to node 两组 2026-09-26 精简时并入 m3 / noderun），M3 的
缓存复用、静音透传、迁移、连线手感与布局，以及 M4 的子图、库算子、live preview、大图性能。
headless CLI 没有界面，它的验收在 `bridge/src/cli.rs` 的 `cargo test` 里。

这套验证在 M1 抓到三个真 bug、M2 抓到两个、M3 抓到四个、M4 抓到六个（见
[m4-acceptance.md](m4-acceptance.md)），没有一个是单元测试能发现的 ——
它们全都要求真实的浏览器、真实的事件和真实的后端。**尤其是拖拽类的项**：
#17/#19/#21 的断言必须用 CDP 的 Input 域发真鼠标事件，还得先把节点按屏幕坐标摆好，
否则测到的是「元素在不在 DOM 里」而不是「手感对不对」。
